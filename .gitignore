/vendor/
/.php-cs-fixer.cache
/.phpunit.cache/
/.phpunit.result.cache

# the library itself, not the app
/videos/*
!/videos/.gitkeep

/thumbs/*
!/thumbs/err.png

# the metadata the app writes about that library, not the app either

/data/*
!/data/.gitkeep
